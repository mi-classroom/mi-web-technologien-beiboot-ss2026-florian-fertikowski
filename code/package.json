{
  "name": "beiboot-gesture-recognition",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=reha-demo",
    "build": "npm run build --workspace=gesture-lib && npm run build --workspace=demo && npm run build --workspace=reha-demo"
  }
}